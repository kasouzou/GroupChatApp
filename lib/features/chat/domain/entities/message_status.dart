/// メッセージの配送・送信状態を表す列挙型
enum MessageStatus {
  /// 送信中（サーバーからの応答待ち。楽観的UI表示などで使用）
  sending,

  /// オフライン等の理由で送信が保留されている状態（再送待ち）
  pendingOffline,

  /// サーバーに正常に受理された状態
  sent,

  /// 送信に失敗した状態（リトライが必要、あるいは永続的なエラー）
  failed,
}
